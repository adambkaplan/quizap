apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - release.yaml

metadata:
  name: shipwright-build
  annotations:
    description: "Shipwright build installation from latest release"
